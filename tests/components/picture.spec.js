import {expect} from 'chai';

describe('Picture Component', () => {
  describe('Click on picture', () => {
    it('should set the current picture', () => {
      expect(null).to.be.null;
    });
  });
});
